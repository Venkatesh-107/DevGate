class GitHubConstants {
  // Use a placeholder client ID for now. User must replace it.
  static const String clientId = 'YOUR_OAUTH_APP_CLIENT_ID';
  static const String scopes = 'repo,read:user';
}
